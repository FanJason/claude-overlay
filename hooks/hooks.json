{
  "description": "Run /overlay locally before the model turn (zero tokens)",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/prompt_submit.py\"",
            "timeout": 45
          }
        ]
      }
    ]
  }
}
